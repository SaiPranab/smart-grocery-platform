server:
  port: 8888 # Standard port for config server

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          # Point this to the absolute path of the config-repo we just made
          uri: file://D:/projects_SPRING/SAI_SIR/smart-grocery-platform/infrastructure/config-repo
          default-label: main